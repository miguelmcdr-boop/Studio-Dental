import { describe, it, expect } from 'vitest'

describe('pagosStorageService — purga (placeholder)', () => {
  it('los tests legacy de abonos fueron movidos a pagosAbonosLegacyService.test.js', () => {
    expect(true).toBe(true)
  })
})
